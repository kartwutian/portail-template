`use strict`;

const config = {
  routes: {
    index: '/',
    get_start: '/get_start',
    api: '/test',
    about: '/about',
    register: '/register',
    login: '/login',
  },
};

module.exports = config;
