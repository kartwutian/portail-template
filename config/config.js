`use strict`;

const config = {
  routes: {
    'index': '/',
    'get_start': '/get_start',
    'api': '/api',
    'about': '/about',
    'register': '/register',
    'login': '/login',
  },
}

module.exports = config;